name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-auth:
    name: Build & Test Auth Service
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Run Tests in Docker
        run: |
          docker-compose -f docker-compose.test.yml run --rm test-auth
      
      - name: Test Report
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Auth Service Tests
          path: services/auth/target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: true

  build-inventory:
    name: Build & Test Inventory Service
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Run Tests in Docker
        run: |
          docker-compose -f docker-compose.test.yml run --rm test-inventory
      
      - name: Test Report
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Inventory Service Tests
          path: services/inventory/target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: true

  build-orders:
    name: Build & Test Orders Service
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Run Tests in Docker
        run: |
          docker-compose -f docker-compose.test.yml run --rm test-orders
      
      - name: Test Report
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Orders Service Tests
          path: services/orders/target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: true
